{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "minio/mc/Dockerfile"
  },
  "deploy": {
    "healthcheckPath": "/",
    "healthcheckTimeout": 10,
    "restartPolicyType": "NEVER",
    "restartPolicyMaxRetries": 0
  }
}
