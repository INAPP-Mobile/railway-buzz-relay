{
  "REDIS_URL": {
    "defaultValue": "redis://${{redis.RAILWAY_PRIVATE_DOMAIN}}:6379",
    "description": "Redis connection URL over Railway's private network. Redis runs unauthenticated because the private network is project-isolated and not publicly reachable.",
    "isOptional": true
  }
}
