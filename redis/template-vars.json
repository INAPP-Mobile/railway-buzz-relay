{
  "REDIS_PASSWORD": {
    "defaultValue": "${{secret(32)}}",
    "description": "Redis password, auto-generated per deploy. The relay reads it via ${{redis.REDIS_PASSWORD}} in REDIS_URL, so rotating it here is picked up automatically on redeploy.",
    "isOptional": false
  }
}
