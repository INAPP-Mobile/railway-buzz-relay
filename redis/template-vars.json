{
  "REDIS_PASSWORD": {
    "defaultValue": "redis",
    "description": "Redis password. Default literal 'redis' chosen so marketplace first-time deploys authenticate out of the box. Rotate this in the dashboard AND update the relay's REDIS_URL to match before going to production.",
    "isOptional": false
  }
}